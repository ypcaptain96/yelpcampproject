<!DOCTYPE html>
<html lang="en">
  <head>
      <!Github addition---->
      <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <! Make site responsive on mobile/tablet---->
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <title>YelpCamp</title>
      <link rel="shortcut icon" type="image/png" href="http://i.imgur.com/c4EDNF1.png"/>
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
      <link rel="stylesheet" href="/stylesheets/main.css">
      <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
  </head>
  <body>
  <nav class="navbar navbar-inverse">
      <div class="container">
          <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>

              <a class="navbar-brand" href="/">YelpCamp</a>
          </div>
          <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
              <li class="<%= typeof page !== 'undefined' && page === 'campgrounds'?'active' : '' %>"><a href="/campgrounds"><span class="glyphicon glyphicon-tent fa-fw"></span>Home</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <% if(!currentUser){ %>
                    <li class="<%= typeof page !== 'undefined' && page === 'login'?'active' : '' %>"><a href="/login">
                        <i class="fa fa-sign-in" aria-hidden="true"></i>  Login</a></li>
                    <li class="<%= typeof page !== 'undefined' && page === 'register'?'active' : '' %>"><a href="/register">
                        <i class="fa fa-user-plus" aria-hidden="true"></i> Sign Up</a></li>
                <% } else { %>
                    <li class="<%= typeof page !== 'undefined' && page === 'user'?'active' : '' %>"><a href="/users/<%= currentUser.id %>"><span class="glyphicon glyphicon-user fa-fw"></span><strong><%= currentUser.username.capitalize() %></strong></a></li>
                    <li><a href="/logout"><i class="fa fa-sign-out fa-fw" aria-hidden="true"></i>Logout</a></li>
                <% } %>
                    <li role="separator" class="divider">
                       <form action = "/campgrounds" method = "GET" class="navbar-form" role="search" autocomplete = "off">
                          <div> 
                               <div class = "input-group stylish-input-group">
                                   <input type = "text" name = "search" placeholder = "Search YelpCamp" class = "form-control" style = "color:white;">
                                   <span class="input-group-addon">
                                        <button type="submit" style = "color:white;">
                                            <span class="glyphicon glyphicon-search"></span>
                                        </button>  
                                    </span>
                               </div>
                           </div>
                       </form>
                    </li>
                </ul>
          </div>
      </div>
  </nav>
       
       
      <div class="container-fluid">
        <div class="row">
            <div class="col-md-6 col-md-offset-5">
                <% if(error && error.length > 0){ %>
                   <div class = "alert alert-danger text-center" role = "alert">
                       <%= error %>
                   </div>
               <% } %>
               <% if(success && success.length > 0){ %>
                   <div class = "alert alert-success text-center" role = "alert">
                       <%= success %>
                   </div>
               <% } %>
                <% if(warning && warning.length > 0){ %>
                   <div class = "alert alert-warning text-center" role = "alert">
                       <%= warning %>
                   </div>
               <% } %>
            </div>
        </div>
     </div>
       
<div class = "container">